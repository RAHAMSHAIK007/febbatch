  yum install docker -y
  sudo curl -L "https://github.com/docker/compose/releases/download/1.29.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
  sudo chmod +x /usr/local/bin/docker-compose
  sudo ln -s /usr/local/bin/docker-compose /usr/bin/docker-compose
  docker-compose --version
  systemcctl start docker
  systemctl start docker
  systemctl status docker
  vim docker-compose.yml
  vim Dockerfile
  vim index.html
  docker build -t paytrain:v1 .
  vim index.html
  docker build -t paymovie:v1 .
  vim index.html
  docker build -t paycric:v1 .
  docker images
  vim docker-compose.yml
  cat docker-compose.yml
  docker ps -a
  docker-compose up -d
  vim docker-compose.yml
  docker-compose up -d
  docker ps -a
  vim docker-compose.yml
  cat docker-compose.yml
  docker-compose up -d
  docker image
  docker images
  vim docker-compose.yml
  docker-compose up -d
  docker ps -a
  docker-compose up -d --scale=5
  docker-compose up -d train --scale=5
  docker-compose train --scale=5
  docker-compose scale
  docker-compose --scale
  docker-compose --scale help
  docker-compose scale movie=2
  docker ps -a
  docker-compose logs
  docker-compose images
  docker-compose ps
    sudo wget -O /etc/yum.repos.d/jenkins.repo https://pkg.jenkins.io/redhat-stable/jenkins.repo
    sudo rpm --import https://pkg.jenkins.io/redhat-stable/jenkins.io.key
 
  amazon-linux-extras install java-openjdk11 -y
  yum install git jenkins -y
  systemctl start jenkins
  systemctl status jenkins
  vim /lib/systemd/system/docker.service
  systemctl daemon-reload
  service docker restart
  chmod 777 /var/run/docker.sock
  docker ps -a
  ll
  docker-compose up -d
  docker ps -a
  docker-compose ps
  docker-compose stop
  docker-compose kill
  docker-compose ps -a
  ll
  mv docker-compose.yml docker-compose1.yml
  ll
  docker-compose up -d
  docker-compose -f docker-compose1.yml up -d
  docker-compose -f docker-compose1.yml stop
  docker-compose -f docker-compose1.yml images
  docker-compose images
  docker-compose -f docker-compose1.yml images
  docker-compose -f docker-compose1.yml up -d
  '
  ll
  mv docker-compose1.yml docker-compose.yml
